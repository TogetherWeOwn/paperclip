name: PR

on:
  pull_request:

permissions:
  actions: read
  contents: read
  pull-requests: read

jobs:
  ci:
    # Execute the reviewed reusable workflow from its immutable published SHA;
    # never trust a workflow implementation from the pull request checkout.
    uses: TogetherWeOwn/paperclip/.github/workflows/pr-trusted.yml@5a6cd509269326cf77b43eaca4f058487c956a12
