name: PR

on:
  pull_request:
    types: [opened, reopened, synchronize, edited]

permissions:
  actions: read
  contents: read
  pull-requests: read

jobs:
  ci:
    # Execute the reviewed reusable workflow from its immutable published SHA;
    # never trust a workflow implementation from the pull request checkout.
    uses: TogetherWeOwn/paperclip/.github/workflows/pr-trusted.yml@8eae81258a45c24e0f4fc70442b7cb81123e6800
    secrets:
      TRUSTED_POLICY_TOKEN: ${{ secrets.TRUSTED_POLICY_TOKEN }}
